[project]
name = "vulnlab-fastapi"
version = "0.1.0"
description = "Minimal FastAPI app with routes from JSON for pentest labs"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.115.5",
  "uvicorn==0.32.1",
]

[tool.uv]
dev-dependencies = []

